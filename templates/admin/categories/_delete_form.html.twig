<form method="post" action="{{ path('categories_delete', {'id': category.id}) }}" onsubmit="return confirm('Voules-vous vraiment supprimer cette catégorie ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger" style="padding-left: 19px; padding-right: 19px;"><i class="btn-label fas fa-trash" title="Supprimer"></i></button>
</form>
